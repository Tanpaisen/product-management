extends ../../layouts/default.pug
include ../../mixins/filter-status.pug
include ../../mixins/search.pug
include ../../mixins/pagination.pug
include ../../mixins/form-change-multi.pug
include ../../mixins/alert-flash.pug
include ../../mixins/sort.pug
include ../../mixins/table-tree.pug

block main
    h1 Danh sách sản phẩm

    +messages(5000)
    .card.mb-3
        .card-header Bộ lọc và Tìm kiếm
        .card-body
            .row
                .col-6
                    +filter(filterStatus)
                .col-6 
                    +search(keyword)
    .card.mb-3
        .card-header Danh mục sản phẩm
            .card-body
                .row 
                    .col-10
                        +formChangeMulti(`${prefixAdmin}/products-category/change-multi?_method=PATCH`)
                    .col-2
                        a.btn.btn-outline-success(href=`${prefixAdmin}/products-category/create`) + Thêm mới  
                +sort()         
        table(
                class="table table-hover table-sm"
                change-multi
            )
                thead
                    tr
                        th 
                            input(
                                type="checkbox" 
                                name="checkall"
                            )
                        th STT
                        th Hình ảnh
                        th Tiêu đề
                        th Mục cha
                        th Vị trí
                        th Trạng thái
                        th Hành động
                tbody
                    +tableTree(productsCategory)
        form(
            action=""
            method="POST"
            id="form-change"
            data-path=`${prefixAdmin}/products-category/change-status`
        )
        //Xóa mềm 1 sp
        form(
            action=""
            method="POST"
            id="form-delete"
            data-path=`${prefixAdmin}/products-category/deleteOne`
        )
        // Xóa vĩnh viễn
        form(
            action=""
            method="POST"
            id="form-delete-perpetual"
            data-path=`${prefixAdmin}/products-category/delete`
        )
        form(
            action=""
            method="POST"
            id="form-restore"
            data-path=`${prefixAdmin}/products-category/restoreOne`
        )


    script(src="/admin/js/products.js")