sider.sider
  .inner-menu
    ul 
      li
        a(href='/admin/dashboard') Tổng quan
      if(role.permissions.includes("products-category_view"))
        li
          a(href='/admin/products-category') Danh mục sản phẩm
      if(role.permissions.includes("products_view"))
        li
          a(href='/admin/products') Danh sách sản phẩm
      if(role.permissions.includes("blogs-category_view"))
        li
          a(href='/admin/blogs-category') Quản lý danh mục bài viết 
      if(role.permissions.includes("blogs_view"))
        li
          a(href='/admin/blogs') Quản lý bài viết 
      if(role.permissions.includes("roles_view"))
        li
          a(href='/admin/roles') Trang quản lý nhóm quyền
      if(role.permissions.includes("roles-permissions_view"))
        li
          a(href='/admin/roles/permissions') Phân quyền
      if(role.permissions.includes("accounts_view"))
        li
          a(href='/admin/accounts') Quản lý tài khoản
      li
          a(href='/admin/settings/general') Cài đặt chung