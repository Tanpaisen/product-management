mixin messages(time)
    if messages.success
        .message.info
            .alert.alert-success(
                show-alert 
                data-time=time
            ) #{messages.success} 
    else if messages.error
        .message.info
            .alert.alert-danger(
                show-alert 
                data-time=time
            ) #{messages.error} 