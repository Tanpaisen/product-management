extends ../../layouts/default
include ../../mixins/box-head
include ../../mixins/products-layout.pug

block main
    h1 Trang chủ
    .container.my-3
        .row
            .col-12
                +box-head('Danh sách sản phẩm nổi bật')
        +products-grid(productsFeatured)

        .row
            .col-12
                +box-head('Danh sách sản phẩm mới nhất')
        +products-grid(productsNew)